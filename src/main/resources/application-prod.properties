# Server port – Railway injects $PORT, fallback to 8081
server.port=${PORT:8081}

# Hardcoded PostgreSQL JDBC URL (with password embedded)
spring.datasource.url=jdbc:postgresql://postgres:TgkdaEfbxtXeHJSQDfcqHiwUmKnjxdFD@reseau.proxy.rlwy.net:30699/railway
spring.datasource.username=postgres
spring.datasource.password=TgkdaEfbxtXeHJSQDfcqHiwUmKnjxdFD

# JPA
spring.jpa.hibernate.ddl-auto=update

# Email – use environment variables (keep these placeholders)
spring.mail.host=${MAIL_HOST}
spring.mail.port=${MAIL_PORT}
spring.mail.username=${MAIL_USERNAME}
spring.mail.password=${MAIL_PASSWORD}

# Stripe API key – environment variable
stripe.api.key=${STRIPE_API_KEY}